ALTER TABLE things
DROP CONSTRAINT things_item_key;

/* This SQL statement uses the DDL sublanguage
to manipulate the data definitions,
specifically to drop a table constraint, part of the
the table schema. */
