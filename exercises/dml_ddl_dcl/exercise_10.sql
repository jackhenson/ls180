CREATE SEQUENCE part_number_sequence;

/* This SQL statement uses the DDL sublanguage
since CREATE SEQUENCE modifies the characteristics
and attributes of the database by adding a 
sequence object to the database structure. */
